# syntax=docker/dockerfile:1

FROM node:22-alpine

WORKDIR /app

COPY package.json ./
RUN npm install

COPY tsconfig.json vite.config.ts env.d.ts index.html ./
COPY src ./src

EXPOSE 3003

CMD ["npm", "run", "dev"]
